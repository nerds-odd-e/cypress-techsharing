{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser edge:dev",
    "start:backend": "./gradlew bootRun",
    "start:frontend": "cd app && yarn build && sleep 3 && yarn preview cd -",
    "test:dev": "run-p -clnr start:backend start:frontend cy:open",
    "test:ci": "run-p -clnr start:backend start:frontend cy:run"
  },
  "devDependencies": {
    "@testing-library/cypress": "^8.0.1",
    "cypress": "^8.6.0",
    "npm-run-all": "^4.1.5"
  }
}
